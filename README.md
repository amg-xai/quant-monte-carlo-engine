# Quant Monte Carlo Engine

A professional-grade stochastic option pricing engine built with Monte Carlo simulation.

## Setup
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```